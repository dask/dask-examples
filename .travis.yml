language: python

python:
    - "3.5"
    - "3.6"

before_install:
    - sudo apt-get install graphviz

install:
    - pip install -r requirements.txt

script:
    - python $TRAVIS_BUILD_DIR/tests/nbrun.py
  
notifications:
  email: false